paths:
  /cryoboxes:                # path of the user from your endpoint
    get:
      summary : get all cryoboxes from the system
      produces:
      - application/json
      responses:
          200:
            description: OK
    post:
      summary : create new cryobox
      produces:
        - application/json
      responses:
          200:
            description: OK
        